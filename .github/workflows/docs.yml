name: Docs

# Build and publish the user documentation site (docs/) to GitHub Pages.
#
# This is a SEPARATE workflow from release.yml (which builds the app binaries on
# a `v*` tag trigger) — release.yml is intentionally left untouched. The app's
# CI (ci.yml) does NOT build the docs, so PR CI on code changes stays fast.
#
# Publishing is driven by pushes to `main` (not `v*` tags): `main` is an allowed
# deployment branch for the `github-pages` environment, whereas tags are not —
# the old tag trigger was rejected by the environment's branch policy on every
# release. Deploying from main also keeps the published docs current with the
# repo instead of only refreshing at release time.
#
# ONE-TIME HUMAN SETUP (cannot be automated from here): set
#   Settings -> Pages -> Build and deployment -> Source -> "GitHub Actions".
# Until that is set, the deploy job has nowhere to publish.

on:
  # Publish whenever the docs (or this workflow) change on main. Path-scoped so
  # unrelated code merges don't trigger a redundant Pages deploy.
  push:
    branches: ["main"]
    paths:
      - "docs/**"
      # …but NOT the internal notes folded in by #3315: `docs/design/` and
      # `docs/plans/` are in `_config.yml`'s `exclude:`, so an edit to one cannot
      # change a published byte. Without these two lines every design-note PR
      # would run a dry-run that proves nothing, and every design-note merge
      # would redeploy an identical site.
      - "!docs/design/**"
      - "!docs/plans/**"
      - ".github/workflows/docs.yml"
  # Manual button to force a re-publish from main at any time.
  workflow_dispatch:
  # Build-only dry-run when a PR touches the docs (deploy is skipped below), so a
  # broken _config.yml or theme pin is caught before it ships.
  pull_request:
    paths:
      - "docs/**"
      # …but NOT the internal notes folded in by #3315: `docs/design/` and
      # `docs/plans/` are in `_config.yml`'s `exclude:`, so an edit to one cannot
      # change a published byte. Without these two lines every design-note PR
      # would run a dry-run that proves nothing, and every design-note merge
      # would redeploy an identical site.
      - "!docs/design/**"
      - "!docs/plans/**"
      - ".github/workflows/docs.yml"

# Least-privilege token scoped to what deploy-pages needs.
permissions:
  contents: read
  pages: write
  id-token: write

# Never run two Pages deploys at once; don't cancel an in-flight publish.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # NB: no actions/configure-pages step here on purpose. That action calls
      # the Pages REST API to discover the site's base path — which 404s until
      # the one-time "Source: GitHub Actions" setting is flipped, and would fail
      # this build (including the PR dry-run) before Pages is enabled. We don't
      # need it: `url`/`baseurl` are set explicitly in docs/_config.yml, so the
      # build is self-contained and provable on a PR without any repo setup.

      # Provides the Jekyll runtime (github-pages gem) — no Ruby/Gemfile in the
      # repo. Builds docs/ into ./_site, resolving the pinned remote theme.
      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./docs
          destination: ./_site

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3

  deploy:
    # Skip publishing on pull requests — PRs only get the build dry-run above.
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
