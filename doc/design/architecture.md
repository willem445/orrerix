# Architecture map

The source tree and its seams. This note is the map every other design note
assumes: what each module owns, and where a new capability is supposed to go.
It lived in the README until #609 moved it here — the README is a pitch, and a
file-by-file map of an 11k-line orchestration module is not one.

Deeper *why* for any individual subsystem lives in its own note in this
directory; the hard constraints (never resize the PTY, no getrandom crates on
the Windows baseline, no live agent testing) live in [`CLAUDE.md`](../../CLAUDE.md).

## The Cargo workspace

The repo root is a Cargo workspace with three members, one `Cargo.lock` and one
`target/`, both at the root:

```
Cargo.toml               workspace root: members, resolver, and [profile.release] — cargo reads profiles from the ROOT manifest only and merely warns about one in a member, so this is where lto/codegen-units/debug/strip have to live (#888 slice A1)
src-tauri/               the desktop app. Links Tauri; owns every #[tauri::command], the capability/ACL manifests, and the Windows desktop surface
crates/loomux-engine/    the orchestration core as a crate that does not link Tauri. Filling up in batches (#888 slice A2), each module re-exported under its old `orchestration::` path so no call site moved with it. Deliberately not enumerated here — it would go stale every batch: `crates/loomux-engine/src/lib.rs` is the running account of what has arrived and what each move cost, and the manifest argues every dependency the crate has taken on
crates/loomux-server/    the remote-engine daemon (#888 slice C1a): the process that will HOST that core on a server. A binary, not a library the desktop links — and a leaf, so nothing in the shipped Windows binary's dependency graph reaches it. Today it is a skeleton: command line, YAML config, and the one decision it makes fail-closed — a config naming a routable bind address does not LOAD unless it also says `allow_routable_bind: true` (remote-engine-protocol.md §1.2's first v1 control, landed a slice ahead of the socket as config-layer validation: `ServerConfig` holds an already-classified `ListenTarget` and its `Deserialize` is on a private raw shape, so C2 receives a checked value instead of a string it must remember to check — the `GroupId` shape applied to a bind address). Linux is the deployment target; the crate builds and tests on all three CI platforms because a security rule that only compiles on the deployment host is one no reviewer can run. No listener (C2), no engine hosting (waits A4). See doc/design/remote-engine-daemon.md
```

`loomux-engine` exists because #888 needs a headless Linux daemon to drive
orchestration, and a server that must build webkit2gtk in order to run is not a
deployment shape. `loomux-server` is that daemon. One rule governs the three:
**the arrow runs `loomux-server` → `loomux-engine` and `src-tauri` →
`loomux-engine`, and never points back**, with everything the engine needs from
its host arriving as a trait (`EventSink`, `PaneHost`) rather than an
`AppHandle`. The
modules below move into it in stages — see
[`engine-extraction.md`](engine-extraction.md) for the boundary, the order, and
the publish stance.

## The source tree

```
src-tauri/src/
  pty.rs            PTY lifecycle (spawn/write/resize/kill) + output streaming; per-kind Terminal shells (PowerShell/cmd/Git Bash, #194) + Git Bash discovery. The input path never blocks under the global `ptys` map lock (#719): the per-pane writer/master are shared handles cloned out under it, and `write_pty`/`change_dir` run their whole body off the webview thread. Since #1607 that body runs on the pane's OWN writer thread (`spawn_pane_writer`, reached through `PtyHandle`'s `writer_jobs` sender — epic #1600 Phase 2.3) rather than the shared `spawn_blocking` pool, which orchestration polling can exhaust; the command awaits a completion reply, so there is still no early-returning backend write queue — back pressure is what bounds memory. See doc/design/pty-input-path.md
  ptyout.rs         per-pane coalescing of PTY output before it crosses IPC (#712): a `pty-output` event costs one one-shot script compilation on the GUI thread, so the emit rate is bounded to one per 60 Hz frame per pane (leading-edge, so a quiet pane's chunk still crosses on arrival) instead of one per `read()` return. Bytes, order and the orchestration output ring are untouched. See doc/design/pty-output-coalescing.md
  sessions.rs       agent session discovery (one collect_*_candidates fn per file-backed source, plus scan_opencode for the one that is a SQLite store); metadata-first + a persisted head-parse index (session-index.json) so a long history costs a stat, not a parse (#493). See doc/design/session-index.md
  orchestration/    agent groups: registry, guardrails, MCP server, audit. Four agent-CLI
    adapters (claude, copilot, gemini, opencode, pi, codex) whose per-CLI differences live as DATA in
    `CLI_CAPS` rather than as `if cli == …` at a call site — that table, the capability
    class (`Role`) and the deny tier it selects (`Containment`) now live in
    crates/loomux-engine (#888 slice A2 batch 4) and are re-exported here, so every
    `orchestration::` spelling of them resolves unchanged. `Role` carries two shared
    predicates (#2519): `is_fixture` — a pane opened FOR the human rather than a delegate
    an orchestrator opened, which is the one exemption rule seven guardrails read (the
    dock, the cap, the reaper, the watchdog, the review driver's release, `spawn_agent`'s
    refusals and persona ownership) — and the narrower `is_root`, the one agent a
    delegate's `report` is typed into, which `deliver_relayed_to_root` looks a group up by.
    They differ by exactly `Role::Manager`, and that gap is load-bearing: doc/design/manager.md.
    `Role::Lead`, a human-driven pane that opens helper panes instead of harness subagents,
    is doc/design/lead-pane.md; opencode's whole seam — an
    env-delivered config document, its permission-key containment and its per-group session
    store — is `doc/design/opencode.md`, and codex's — a whole config document layered by
    `-p/--profile` into the user's own `CODEX_HOME`, carrying the project trust that keeps a
    pane off the trust dialog, the approval posture, the MCP server and the role contract —
    is `doc/design/codex.md`. codex is the one adapter whose generated file lands outside
    loomux's state root, so it is brand-namespaced, removed with its agent, and swept at
    startup by the same `sweep_orphaned_agent_files` described below. Compact-survival is
    layered (#329, #416, #417): a durable role CONTRACT riding the CLI's own system-prompt layer
    (a generated `~/.claude/agents`/`~/.copilot/agents` custom-agent file on both CLIs — Claude's
    own inline `--agents` JSON flag was replaced with a file in a later correction round after a
    live demo hit Windows CreateProcessW's 32,767-character command-line limit) is the primary
    defense against compaction diluting it; `end_group` reclaims a group's own generated files when
    it ends cleanly, and a one-shot startup sweep (`sweep_orphaned_agent_files`, #464) reclaims any
    left behind by a group that never reaches `end_group` at all (a crash, or — the case that
    actually filled a real `~/.claude/agents` with 1,111 stray files — a test run, whose ability to
    write there at all is itself fixed at the source in #502); both Claude's `PreCompact`/`SessionStart` hooks AND Copilot's own
    `preCompact` hook are TRUSTED evidence sources for detecting a compaction (no banner/
    token-drop guessing needed once configured) — Claude additionally gets a native
    `SessionStart(compact)` re-grounding signal Copilot's docs confirm has no equivalent, so a
    Copilot compaction always closes via loomux's own reinjection instead. Both CLIs have a real
    `/compact` command loomux can nudge at a natural lull or on request (Copilot's confirmed via
    its own CLI command reference, one tick later than Claude's since it lacks that native
    re-grounding signal); banner/token-drop/marker inference remains the fallback tier for
    hook-less setups. Since the CONTRACT itself now durably rides the system-prompt layer for
    almost every agent, the mandatory post-compact re-grounding notice is SLIM by default (a
    re-sync pointer to the task board/durable state/roster, plus the directive ledger tail
    inlined) rather than re-embedding instructions the agent's system prompt already holds
    permanently — full verbatim re-embedding is now reserved for the one documented case where
    a Copilot block's contract does NOT ride `--agent` (a user-authored native
    `.github/agents/*.md` persona). That mandatory notice is considered delivered when the AGENT
    acknowledges it — its own next MCP call — not when loomux's submit sampler happens to see the
    Enter (#535): the sampler misses routinely on a busy/repainting pane, and keying on it re-sent
    a re-grounding that had already landed, interrupting a working agent. A retry is also never
    spent into a live turn (deferred while output is flowing, bounded so a permanently noisy pane
    can't suppress it); the 3-attempt bound and the visible give-up for a genuinely lost
    re-grounding are unchanged. Every surface that reports a finished re-grounding says WHICH
    evidence closed it and claims only what that evidence proved (#546) — `ReinjectAck` owns the
    vocabulary once, and the badge (`re-grounding delivered` vs `re-grounding unproven (agent
    alive)`) and the audit (`compact-reinjection-confirmed` vs
    `compact-reinjection-liveness-only`) both derive from it. The liveness arm proves the agent
    is alive and working, never that it read the re-grounding and not even that the paste
    arrived; neither arm proves the read, because nothing loomux can observe from outside a
    session does. The same activity signal now also
    feeds the unconfirmed-delivery detector (#539): a pane whose box no longer holds our paste
    AND whose agent has called a loomux tool since is recorded as busy rather than announced as
    stranded, and the alarms that remain coalesce per pane into one notice naming every
    delivery. See doc/design/orchestration.md's Compact-nudge section and "The
    unconfirmed-delivery detector gets evidence about the AGENT".
    workflow.rs     MOVED to crates/loomux-engine/src/workflow.rs (#888 slice A2 batch 5) and re-exported here, so `orchestration::workflow` still resolves. It moved as a cluster with `profiles` and `locks` — `profiles` calls `workflow::kind_from_str`/`resolve_profile_path` while `parse_workflow` calls `profiles::sanitize_allow`, a cycle that is legal inside one crate and unrepresentable across two, so no batch order moved either alone. The block model (#222): a repo's agent roster as data — `<repo>/.orrerix/workflow.yml` parse + validation, with the legacy `.loomux/` spelling still discovered per file and never renamed (#1153 phase 4, doc/design/rebrand-filesystem.md). A block's id is the agent's identity; `kind` is its CAPABILITY CLASS, and stays a closed enum — 5 declarable variants since #1161 added `manager`, the human's own interface (doc/design/manager.md) — so a repo file can declare five reviewers with five prompts but can never grant one write access. `manager` is capped at one per file, refuses `prompt:`/`profile:`/`allow:` exactly as `orchestrator` does, and is not spawnable by any agent. An optional `role_hint` (#250/#324/#891: `advisor` | `process` | `liaison`) rides alongside `kind`, mostly for persona/template/badge selection; `liaison` is SUPERSEDED by `kind: manager` (#1161) and still parses unchanged — every rule below still reads it, and the workflow pane raises a `role-hint-superseded` warning so a file's author is told where the feature went; a repo can never author what a hint MEANS (it picks from a closed set, loomux's code decides the effect). Four rules read one today and they do not all point the same way: two narrow the class the hint sits on — `session_digest` offered to `process` workers alone, `review_verdict` withheld from a `liaison` reviewer — and two widen it for that same liaison, both orchestrator-only for every other hint-carrying class: `group_usage` (#891 S2) and `ask_human` (#1091 slice E, the POSE only — `withdraw_question` settles a row and stays orchestrator-only). `Role::Lead` (#2519) also holds `group_usage`, through its own enumerated surface rather than any hint — no hint can reach that class, since no workflow file can name it. The complete enumeration, narrowing and widening alike, is the table in doc/design/liaison.md. Also the ENFORCED merge gate (#222/#197): reviewer-attributed verdicts (pass | fail | escalate) as durable state, and the pure gate decision the `gh` shim mirrors — `gh pr merge` is refused until every reviewer the gate names has recorded a pass, and no human grant or autonomous marker can open it. See doc/design/workflows.md and doc/design/supervisor-skills.md
    lessons.rs      MOVED to crates/loomux-engine/src/lessons.rs (#888 slice A2 batch 3) and re-exported here, so `orchestration::lessons` still resolves. Its one remaining edge into mod.rs was `tail_snippet`, a pure char-boundary-safe string cut, which moved ahead of it into the engine's `text` module rather than being reached back for. Durable per-repo lessons (#268): `<repo>/.orrerix/lessons.md` (legacy `.loomux/` still discovered, #1153), a plain-Markdown convention file (no schema, no MCP write tool — edited and reviewed like any other file) read-and-capped into the orchestrator's kickoff only. Hard byte cap with oldest-drop truncation, wrapped in a data-not-instructions provenance framing (#189) — never grounds to bypass the merge gate. See doc/design/lessons.md
    profiles.rs     MOVED to crates/loomux-engine/src/profiles.rs (#888 slice A2 batch 5) and re-exported here, so `orchestration::profiles` still resolves — see the `workflow.rs` entry above for why the two could only move together. Repo-authored personas from `.github/agents/*.md` (#51, harvested from PR #105): append/replace modes with a non-overridable loomux mechanics core. Compiled to each CLI's native custom-agent flag — `claude --agent`/`copilot --agent` (both resolve a NAME against a user/project agent-file directory; a user-authored file resolves unwrapped) — and (#416) the built-in role contract itself now rides the SAME native flag for every block, persona or not: a block with no user-authored file gets a loomux-generated file in the CLI's own user-level agent directory (`~/.claude/agents` or `~/.copilot/agents`, never the repo's `.github/agents/`). Claude's inline `--agents '<json>'` flag was the original #416 mechanism; it was replaced with the same file-based approach Copilot always used after a live demo hit Windows CreateProcessW's 32,767-character command-line limit once the full contract, not just a short persona, rode it
    groupid.rs      MOVED to crates/loomux-engine/src/groupid.rs (#888 slice A2 batch 2) and re-exported here, so `orchestration::groupid` / `orchestration::GroupId` still resolve. The validated group identifier (#904, #888 §0 layer 2). CLAUDE.md constraint 6 USED to trust `group_id` as a path segment because the caller is our own in-process webview — a fact about the TRANSPORT, not a credential, and one a network client would not supply; #904 rewrote it. `GroupId` moved that trust onto a type: one constructor (`parse`), a strict ASCII alphabet that makes `..`, separators, drive letters, control bytes and non-ASCII unspellable, and no `From<String>`/`new_unchecked` — deserialization goes through the same gate, so a hand-edited state file cannot mint one either. Deliberately not `AsRef<Path>`: an id becomes a path only inside `group_dir_at`, the one declared root helper, which `OrchRegistry::group_dir` is the `&self` convenience over — the `~50 orch_*` commands parse their raw string once at the boundary (`command_group`) and thread the type from there, and a source-scanning test pins that no second join grows back. That test walks BOTH source roots, not just `src-tauri/src`: the orphan rule puts the only legal `impl AsRef<Path> for GroupId` in whichever crate owns the type, so once the type moved, a single-root scan would have been green forever while enforcing nothing. Refuses, never rewrites. Two shapes the type ruled out on contact and that are worth knowing before you reach for them: `AttentionItem.group` stays a `String` (it is legitimately EMPTY for a plain pane, which a `GroupId` cannot be), and `QueuedDelivery.group` is an `Option<GroupId>` (there is no default group, and a pre-#468 snapshot entry without one must still PARSE so its payload surfaces as an orphan — `None` says "no recorded identity" in the type instead of via an empty string). See doc/design/groupid-and-path-roots.md
    rootreg.rs      NOT in this tree — `crates/loomux-engine/src/rootreg.rs`, born there rather than moved (#1042 slice A), and listed beside `groupid.rs` because it is the other half of the same sentence: a `GroupId` says a string is a usable path SEGMENT, a `DeclaredRoot` says a path is a ROOT somebody declared. `RootRegistry` holds the canonicalized set of roots trusted sources have declared (the local webview, the engine's own derivations, later the daemon's config) and `resolve` is the only way to mint a `DeclaredRoot`: it accepts a candidate whose canonical form EQUALS or is a DESCENDANT of a declared root and refuses everything else — an ancestor (grants more than was declared), a sibling whose name merely extends one, a drive-relative `C:foo`, a `..` component, and a link whose target leaves every declared root. `DeclaredRoot { plain, canonical }` carries both forms because Windows canonicalization returns `\\?\C:\…`: right as the comparison key, wrong as a git subprocess' cwd — `as_path()` (the one accessor, deliberately not `AsRef<Path>`) hands back `plain`. Never persisted: a registry file would be the replay-poisonable artifact this design exists to defeat, so it is rebuilt from trusted sources every boot. **Slice B populates it** (`src-tauri/src/rootreg.rs`, below) and **nothing resolves against it yet**: boundary `resolve` on the `ft_*`/`fm_*`/`git_*`/`gh_*` families is slice C. See doc/design/groupid-and-path-roots.md
    humanq.rs       the human-question registry (#946): `questions.json` in the group dir, plus `ask_human`/`list_questions`/`withdraw_question`. Exists because a blocking orchestrator question is a fleet outage — a pane showing its CLI's own modal cannot take ANY delivery, so every worker report queues behind it — and `ask_human` returns an id immediately instead. The pending record is ENGINE state, not a liaison agent's: an agent pane compacts, wedges and dies, which would re-create that outage one level up; a presenter (webview inbox, liaison pane, #947 chat bridge) is a client of the record, never the record. The trust boundary is the point, and it is drawn between ASKING and ANSWERING rather than between roles: every agent may ASK — the orchestrator and (since #1091 slice E) a `liaison`-hinted reviewer through `ask_human` itself, every other delegate through `message_orchestrator` — and no agent may ever ANSWER — `call_tool` is a closed match and no arm reaches `answer_question`, `AnswerSource` is a closed enum the ENTRY POINT supplies (so `orch_question_answer` hard-codes `Webview` rather than taking a `source` string), and both halves are pinned by a behavioural sweep plus a source scan. Reads are loud about a malformed file, deliberately unlike `tasks()`: every mutation is a whole-file read-modify-write, so "unparseable reads as empty" would let the next ask destroy a human's outstanding questions. See doc/design/human-questions.md
    needsyou.rs     the needs-you item registry (#1151): `needs-you.json` in the group dir beside `questions.json`, plus the raise/withdraw/resolve/list surfaces and the `needs-you-cleared` watermark marker. Exists because the NEEDS-YOU panel's demo rows WERE the task — a pure projection of demo-gated `tasks.json` rows — so the thing a human was being asked to look at had no identity, no timestamps and no close-out that was not also a board move. An item LINKS a task and never copies one: `demo_path`/`pr`/`assignee` are joined live at render, because snapshotting board fields onto an item is the drift machine the projection was written to avoid. Deliberately NOT folded into `humanq`: `questions.json` is a shipped contract with its own trust boundary, and resolving (the human has looked) is a different power from answering (a decision that releases held work) — the panel unions two registries instead. Lifecycle is mapped from the board by one hook in `upsert_task`, keyed on the TRANSITION into/out of `DEMO_GATED_STATUSES` so a note on an already-parked row raises nothing; dedupe lives in one pure function (`admit`) so the hook, the one-shot upgrade migration and an explicit agent raise cannot disagree about "already raised" — with the one difference between them a named `Dedupe` scope, because a raise treats a settled row as a closed episode (a re-parked task deserves a new one) while the migration treats it as proof the task is already accounted for (a human's close-out must never be undone by a migration re-running). That migration is a marker-guarded once-ever step at GROUP LOAD, never on a read: `orch_needs_you_list` is `viewer`-tier and must not be able to write a file, emit an event or grow the audit log from a poll. Reads are loud about a malformed file for `humanq`'s reason; the watermark deliberately fails the OTHER way (unparseable reads as 0, showing more rather than hiding). Resolve is webview-trusted with a closed `ResolveSource` and no MCP arm. See doc/design/needs-you-items.md
    mailbox.rs      NOT in this tree — `crates/loomux-engine/src/mailbox.rs`, born there rather than moved (#1161 M2), and re-exported here so `orchestration::mailbox` resolves. The manager's durable pull registry: `mailbox.json` in the group dir on `humanq.rs`'s idiom — rows the ORCHESTRATOR writes (`message_manager`) and the MANAGER alone reads (`check_mail`), plus the caps and the projection. It exists because the manager pane takes NO mid-session delivery at all: `deliver_prompt` refuses a `Role::Manager` target, which is the feature (no prompt automation in the human's own conversation), so a channel into it has to be pulled rather than pushed. The unread cap is on the UNREAD side and refuses the WRITER — never evicting an unread row to make room, because dropping the human's status stream to preserve an orchestrator's ability to keep writing into a mailbox nobody reads is backwards. Reads are loud about a malformed file for `humanq`'s reason; `mailbox_unread` is the one deliberately quiet read, because it feeds a badge and has no error channel. See doc/design/manager.md
    digest.rs       session-digest friction extraction (#250/#324): one normalizer per agent CLI (Claude `.jsonl`, Copilot session-state, OpenCode `message`/`part` rows) turning a transcript into one event stream, then reduces it, deterministically and LLM-free, into "friction windows" (a tool error, a near-duplicate rerun, a test that went red-then-green, a reverted edit) — the `session_digest` MCP tool a `role_hint: process` block calls to mine a finished session cold. See doc/design/supervisor-skills.md
  obs.rs            MOVED to crates/loomux-engine/src/obs.rs (#888 slice A3 batch 7), re-exported here so `crate::obs` still resolves; the ~20-line Tauri startup-notice surface is retained. Crash observability: panic hook, breadcrumb log, unclean-exit notice; `data_root()` — the `<data dir>/orrerix` root, overridable via `ORRERIX_DATA_DIR` (or the legacy `LOOMUX_DATA_DIR`, #1153) (#394) for an isolated profile; `init_data_root()` performs the one-time `loomux`->`orrerix` move at startup and NOWHERE else, see doc/design/rebrand-filesystem.md. The split point is the file's own `next-launch notice (Tauri surface)` section marker, which predates the extraction: `StartupNotice` + the `take_startup_notice` command are the only items naming `tauri::`, and everything above the marker is `std` + `dirs`. `install_panic_hook` takes the app version as an argument rather than reading `env!("CARGO_PKG_VERSION")`, because that macro would name the engine crate (deliberately versioned `0.0.0`) after the move and every crash log would have quietly stopped carrying the loomux release version doc/design/crash-observability.md promises. `LockExt`/`lock_safe` — the poison-tolerant `Mutex::lock` — is here rather than behind a host trait because it is an inline extension trait on `std::sync::Mutex`, unusable through a trait object as called; see doc/design/engine-extraction.md §6
  voice.rs          voice prompts (#58): mic capture (cpal) -> local whisper.cpp subprocess
  blocking.rs       the shared P1 delegation helper (#746) and, since #1601 Phase 0.3, `spawn_counted` — the ONE door onto the blocking pool and the only `tauri::async_runtime::spawn_blocking` call in this tree, so the in-flight depth a hang report cites is complete rather than as complete as whoever wrapped the last hand-off remembered. The ticket is taken at the HAND-OFF and moved into the task, so the count includes work still queued behind a full pool — the number a starved hand-off dies behind (beta6's victim was `write_pty`; since #1607 the pty input path is not in this pool at all). `src-tauri/tests/selfwatch.rs` pins the one-door property with its own vacuity controls. See doc/design/self-observability.md
  liveness.rs       the webview half of the liveness heartbeat (#1601 Phase 0.4): one sync `#[tauri::command]` (performance.md §4 X7 — it must NOT delegate, because the blocking pool is one of the two things it measures) stamping what only the webview can measure: its own timer lag and how late a frame was serviced. The backend half is `loomux_engine::selfwatch`, and the divergence between the two stamps separates a stuck GUI thread from a starved backend — the distinction beta5 and beta6 each cost a release cycle to make by hand. A hidden window still stamps and says so, and the verdict answers `GuiHidden` (no evidence) rather than `GuiStuck`. Frontend: `src/liveness.ts`. See doc/design/self-observability.md
  uistate.rs        durable UI state: atomic tabs.json store (project tabs #63) + settings.json store (app settings #370) + sshprofiles.json store (SSH connection profiles #887, hostnames/ports/identity-file PATHS only -- never a credential) + boardprefs.json store (task-board view preferences #1270, one record per group -- collapse state and filters; the group id is a JSON map key, never a path) -- same atomic-write/corrupt-quarantine primitives, opaque JSON owned by the frontend schema
  fileedit.rs       file-editor overlay (#174): lazy tree, read/write (atomic + hash conflict), streaming gitignore-aware search/replace (#207) + path-only name enumeration (#214); server-side path safety
  filemgr.rs        file-MANAGER pane (#214): list, new file/folder, rename, delete-to-Recycle-Bin, open-with-default-app, open-with chooser, reveal-in-OS-file-manager; reuses fileedit's path choke point. Shell APIs come from the `windows` dep we already have (ShellExecuteW + SHFileOperationW)
  filehash.rs       file hashing (#214): SHA-256/512, SHA-1, CRC-32/16/8 — streamed off-thread on a worker (never the main thread), cancellable via the #207 registry
  rootreg.rs        the HOST half of the declared-root registry (#1042 slice B) — who may MINT a declared root, deliberately one small file so "these are all the ways a root gets declared" is one screen to audit (and `tests/rootreg.rs` scans the workspace to keep that sentence true). `admit_root` is the display-side door, classified **`disabled`**: off the wire roster and advertised as absent, which is the whole enforcement — a remote peer may USE declared roots and can never MINT one. It exists because `pickDirectory` is a client-side dialog the backend never sees, so server-declared roots without an admit path would break desktop browsing outright. The two engine-derived declarations live at their sources instead (a group's checkout on create AND resume; a worktree the engine cut — a SIBLING of the repo, so no descendant rule reaches it), and the registry itself is owned by `OrchRegistry` and `manage`d from lib.rs as the same `Arc`, so the engine-side populator has no `set_roots` to forget. See doc/design/groupid-and-path-roots.md
  winpath.rs        MOVED to crates/loomux-engine/src/winpath.rs (#888 slice A4 batch 13); `src-tauri/src/lib.rs` re-exports the whole module (`pub use loomux_engine::winpath;`) rather than keeping a local shim file, since nothing Tauri-shaped stayed behind. Brings `winreg` into the engine manifest, target-gated to mirror `src-tauri/Cargo.toml`'s identical section — already a direct src-tauri dependency and already in the lock, so no new package joins it. Fresh PATH/PATHEXT resolution (registry-merged, so a CLI installed mid-session is findable) + the `which`-style resolver shared by "open in editor" and direct-CLI spawn. Also resolves what the gh/git gate shims bake in: the absolute `sh.exe` (#335) and, from the same install layout, the POSIX coreutils dir the gate normalizes with (#509) — derived, never hardcoded. See doc/design/shim-path-integrity.md
  command_manifest.rs  single source of truth for the ACL manifest's app-command names (#363) — shared by build.rs (include!, feeds the app manifest) and lib.rs (feeds tests/acl_manifest.rs, the coherence guard). Don't hand-count it, and don't restate the total here: tests/acl_manifest.rs's `app_commands_len_is_<N>` pins it, where N IS the count, so the test's own name and assertion message carry both the current number and its per-delta provenance. A count written into this line instead is one nothing keeps true — it went stale at 144 while the manifest held 146 (#1018). See doc/design/acl-manifest.md
  lib.rs            Tauri wiring
src-tauri/
  capabilities/     ACL grants (#363): default.json grants `main` every command via the `main-ui` permission set; plugin-zero-template.json is the zero-grant template a future #360 plugin webview binds to
  permissions/      hand-authored module/tier permission sets (permissions/sets/*.toml) + Tauri-generated per-command allow-/deny- pairs (permissions/autogenerated/*.toml, DO NOT EDIT)
src/
  transport.ts      the ONE module that imports @tauri-apps: the `EngineTransport` seam (invoke/listen + host version, folder picker, close gate). Installable (`setEngineTransport`), so the frontend is mockable in `node:test` and a future remote engine (#888) is a second implementation, not a second frontend. Enforced by test/transport.test.ts -- see doc/design/engine-transport.md, and doc/design/remote-engine-protocol.md for the wire contract and threat model that second implementation would speak (H1-H9 resolved; no listener code yet).
  pty.ts            typed bridge to the backend (invoke + event bus), over transport.ts
  theme.ts          the one palette (#879): the slate/mist ground, the eight named hues, the THREE colour channels they serve (state / interaction / identity -- what separates the channels is POSITION, since the eight-hue set collapses under colour-vision deficiency and the four state dyes must not), the elevation ladder, the type roles, and the xterm ITheme built from them. Colour lives in three languages that cannot see each other -- styles.css's `:root`, index.html's pre-paint block (which runs before the bundle), and the ITheme (terminals are a WebGL canvas that custom properties never reach) -- so this module is the source and test/theme.test.ts pins the other two to it. Rationale and the rules every surface obeys: doc/design/ui-redesign.md (DOM-free, unit-tested)
  pane.ts           one pane: xterm instance + header UI -- or, for a CONTENT pane, a PTY-less surface: file manager (#214), file editor or git view (#217). Also `key` + `facts()` (#2122 slice A1): a per-window identity and one plain-data projection of everything a summarising view needs, carrying the same contract `tabPaneInfo()` does -- no geometry, no IPC, no timer, safe on a hidden tab. See paneactivity.ts / agentrows.ts
  heldbadge.ts      pure delivery-held badge presentation mapping (#246): reason -> header-chip label, for the moment loomux is withholding a prompt because it believes the pane's box is human-occupied, or (#420) because an interactive question/permission TUI is on screen and a blind Enter would silently answer it (DOM-free, unit-tested)
  humanorigin.ts    pure short-lived "this PTY write came from a human" latch (#518): marked by `term.onKey` and the two `term.paste()` sites, read synchronously in `onData`, closed at the end of that turn. Terminal-manufactured data (OSC/DCS query replies, device attributes, focus reports) is emitted in a different turn and reads false — a structural origin test, not a byte-shape guess at an open set of reply shapes (#440 B2-R's argument, applied to the backend's keystroke clock). A second, DEFERRED mark covers the two human paths `onKey` never sees — an IME commit (which xterm sends from a `setTimeout(0)`, a later task than the `compositionend`) and the `insertText` path (dead keys, soft keyboards) — marked from capture-phase textarea events on an ancestor and closed over two timer hops, so no registration order can let the close beat xterm's send (#528 B1: a one-hop close did exactly that, classifying every IME commit non-human). Rides through `ptywrite.ts` to `write_pty`'s `human` flag (DOM-free, unit-tested with real timers for the ordering)
  grid.ts           split-tree layout, dividers, focus, drag/maximize/minimize
  layout.ts         pure drag-reorder geometry (unit-tested, DOM-free)
  paneequalize.ts   pure split-weight math (#936): a newcomer joins a same-direction split at the MEAN of the weights already there, and a closing pane's weight is handed to its survivors in equal absolute parts (total preserved). Together they keep a layout nobody has dragged exactly even through any sequence of splits and closes -- the "even matrix" grid.ts always claimed but did not deliver, and the reclaim that stops a closed pane's space landing entirely on whichever pane was already biggest. DOM-free, unit-tested. See doc/design/pane-equalize.md
  paneautosize.ts   pure weight math for the Autosize gesture (#936): every node weighted by the number of PANES under it, which is what makes the leaves equal — weighting each split's children equally does not, since a pane's size is the product of its share at every level. Takes the tree's shape only (no panes, no elements, no measurement) and returns a WeightNode tree for applyLayoutWeights. Complements paneequalize.ts rather than repeating it: that keeps a row even WITHIN one split, this levels across NESTING, where a pane beside a stacked pair is a half and two quarters however even each split is on its own. On demand only -- nothing re-levels a layout by itself, and reading the shape rather than the weights makes it indifferent to whichever split policy ships. DOM-free, unit-tested. See doc/design/pane-autosize.md
  splitfloor.ts     pure split placement-policy weight math (#885): `halve` -- the target pane pays for the newcomer out of its OWN weight, so the row's total is unchanged and every sibling keeps its exact grow/total RATIO. A ratio guarantee, not a pixel one: the insert adds a divider, which is itself a flex sibling taking ~4px off the row's free space, so each sibling shifts by its share of those pixels -- ~1px in a typical row, below a terminal cell for most splits, but a sibling sitting near a cell boundary does lose a column and repaint. Every HUMAN split gesture uses it. Its `share` branch (the pre-#885 1/N-on-top insert) is NOT what grid.ts routes programmatic placement through any more -- that goes to paneequalize.planEvenInsert, which is both the even matrix #936 actually delivers and what puts the row through the #954 clamp. DOM-free, unit-tested. See doc/design/pane-splitting-and-floors.md
  tabs.ts           project tabs (#63): TabManager -- tab list, active tab, routing (DOM-free)
  workspace.ts      one tab = a Grid + its own dock; hide/show, GL policy, preview composite
  tabbar.ts         the tab strip: switch/close/new, rename, color, alert chips, deterministic agent counter + orchestration markers (#194), preview
  tabroute.ts       pure tab routing + preview scale/sanitizer (unit-tested, DOM-free)
  tabstore.ts       pure encode/decode + schema validation of the persisted tab set (tabs + per-tab pane layout + restore pref, #194; a tab's group BINDINGS are a set and each orch pane records its own group, #485)
  settings.ts       durable app-wide settings (#370): pure encode/decode (DOM-free, unit-tested) + an in-memory singleton (getSettings/setSettings) for synchronous reads from pane.ts's keydown handler. Config-file-only -- no Settings UI exists yet
  sshprofile.ts     SSH connection profiles (#887): pure encode/decode + per-entry validation of sshprofiles.json (DOM-free, unit-tested). Allowlist in BOTH directions and an identity-file path guard, so the no-secrets invariant is structural -- a credential cannot survive a load/save cycle. OWNS the `RemoteShell` value set (type + list + default). See doc/design/ssh-panes.md
  boardprefs.ts     durable task-board view preferences (#1270): pure encode/decode + per-group validation of boardprefs.json (DOM-free, unit-tested). LRU-bounded at 50 groups, and unknown filter families round-trip verbatim so a new one is a key rather than a migration. Frontend-owned view state, never board data. See doc/design/board-tree-view.md
  sshcommand.ts     pure ssh argv builder for SSH panes (#887): option flags, the end-of-options separator, and ONE quoted remote-command string per declared remote shell (posix single-quoting / cmd.exe double-quote doubling, both adversarially unit-tested), plus the fresh->resume rewrite a reconnect uses. Takes flat primitives, never the profile type, and its `program` parameter is the fake-ssh test seam. DOM-free, unit-tested. See doc/design/ssh-panes.md
  restoredecision.ts pure restore-vs-fresh-vs-ask decision for the boot splash (DOM-free, unit-tested, #194)
  panerestore.ts    pure per-pane restore policy + layout-tree -> ordered rebuild plan + agent resume-command builder (DOM-free, unit-tested, #194)
  restoresplash.ts  cold-boot "restore last session?" overlay (thin DOM over restoredecision.ts, #194)
  tabcounts.ts      pure per-tab live-agent counter + live/dormant orchestration markers (DOM-free, unit-tested, #194)
  groupresume.ts    pure whole-group resume plan: orchestrator first, delegates rejoin-or-skip, and ONE plan is ONE group -- members are partitioned by their own recorded group, a member naming another group is refused, an unattributable set fails loudly (DOM-free, unit-tested, #194/#485)
  sessionreconcile.ts pure post-start session-id adoption matcher (refuses on any ambiguity) + the dormant card's "resume last session" candidate lookup (DOM-free, unit-tested, #440). See doc/design/session-id-learning.md
  paneactivity.ts   pure per-pane activity reducer (#2122 slice A2): what the pane has been OUTPUTTING (bytes inside a 4 s window), when the human last typed into it, and the at-prompt LATCH. Clock injected, DOM-free, unit-tested. The latch is the design call: the backend's `waiting` reason already means "parked at a prompt" but is ACKED ON FOCUS, so reading it straight would flip a still-parked pane back to working the instant anyone clicks it. A `waiting` sighting latches; it clears only on evidence independent of that signal -- human input, or output above the floor in one window. The human-input feed hangs off `markFirstInput` AND `markHumanInput` and never `term.onData` (#440 B2-R), which is why copilot's boot OSC replies cannot un-park a pane. See doc/design/agents-tab.md
  agentrows.ts      the plain-data contract between a pane and the two views that summarise it -- the Agents tab (#2122) and the pane Notes rows (#2116). `PaneFacts` (what `Pane.facts()` projects), `deriveAgentState` (the dead/dormant/held/attention/question/reported/turn-done/idle/working precedence ladder, first rung that decides wins), and the row projection + filter/sort/needs-you count. DOM-free and clock-free -- every time-dependent reading is already resolved by `PaneActivity.snapshot`. Imports `attention.ts`'s urgency rather than re-listing it. See doc/design/agents-tab.md
  panefit.ts        pure "hidden => no PTY resize" decision (the no-resize invariant)
  resizeburst.ts    pure "how long to wait before fitting" policy (#1149), the other half of panefit.ts: trailing-edge, so a BURST of geometry changes -- an animated #sessions or .sidedock toggle, a divider drag, a window resize -- collapses into ONE fit at the settled size instead of one per frame, with a ceiling (measured from the burst's start, so the release is the clock and not the burst signal) that keeps a gesture with no settled geometry reflowing. Lives in the debounce rather than in per-gesture brackets, so every consumer of the resize path inherits it -- the shape #432's beginResizeHold/endResizeHold could not cover, since a CSS transition has no start/end to hook. DOM-free, unit-tested. See doc/design/xterm-resize-reflow.md
  paneheader.ts     pure pane-header overflow policy (#2191): from the header's own content width, the fixed chrome's natural width, each control's measured width and its priority flag, which controls stay inline and which fold into the one `⋯` menu, plus where that menu sits (`menuLeftFor` -- left-aligned to the trigger, FLIPPED to right-aligned at the container's edge). Nothing here touches the PTY: the menu is an OVERLAY, because a header that wrapped to a second row would shrink `.pane-term` and repaint the scrollback (constraint 1). Its decision is deliberately independent of its own output -- the title contributes a constant floor rather than its shrunk width, and a folded control is still LAID OUT (`visibility: hidden`, never `display: none`) so it measures the same in both states -- which is what makes the hysteresis a second line of defence rather than the only one. DOM-free, unit-tested. See doc/design/pane-header.md
  panethrottle.ts   pure flush policy for a VISIBLE but unfocused pane's PTY output (#720): batch into one write per window so xterm renders it ~6x less often (renderRows coalesces within an animation frame, never across one); leading-edge, so a quiet pane is never delayed, and byte-exact -- only the moment of the write moves. DOM-free, unit-tested. See doc/design/pane-render-throttle.md
  webglretry.ts     pure BOUNDED backoff for re-acquiring a lost WebGL context (#720): a context is a capped resource, so one pane re-acquiring evicts another's and an unbounded retry live-locks between panes. 2s/10s/60s then stop; a hide/show or a 5-minute healthy lifetime opens a fresh streak. DOM-free, unit-tested. See doc/design/pane-render-throttle.md
  sessions.ts       session browser sidebar: source/role chips, and (#1) each session's recorded task/goal, repo, branch, and PR (when the board has one) — absent rather than guessed for a session predating the field
  sidedock.ts       the right-side dock (#1020 item 6, #934): git / files / editor in one collapsible panel down the RIGHT edge of #workspace, pointed at the active pane's folder. An in-flow FLEX SIBLING of #grid-area since #1150 -- the mirror of sessions.ts on the other edge -- so opening it autosizes the open panes instead of covering them. A TOGGLE adds nothing to the resize path: the panes' own ResizeObservers see the row change and resizeburst.ts collapses the animated burst to one fit per pane, which is what made a displacing panel affordable. Only the grip drag is bracketed (begin/endResizeHold, #432 -- a drag never settles); the follow path still resizes nothing at all. The dock DOES write its own width when the ROOM changes (`roomObs`, so a squeezed column narrows the panel instead of cropping it), and a room that is itself animating can chain onto the dock's ease past the coalescer's ceiling -- open as #1203, argued in doc/design/side-dock.md. Owns its own view instances (app-scoped, not a pane's) and is the one editor-buffer holder outside every pane, so main.ts's quit sweep concatenates its report. See doc/design/side-dock.md
  sidedockmodel.ts  pure side-dock decisions (DOM-free, unit-tested): where the dock points (`decideFollow` -- a closed dock does NOTHING, keeping no pending root, so opening it re-reads the live cwd; a pane with no local cwd never blanks it), which signals may move it at all (`isActiveTabChange` + `followsPaneChange` -- the two doors: TabManager.onChange is a tab-SET listener that also fires on attention/rename/reorder, and Grid.setActive's callback is wired per WORKSPACE so a background tab's pane change reaches it too. A follow re-reads the LIVE cwd, so either one ungated adopts a stale `cd` at a moment the human did not cause -- reading the right pane is not the same as reading it at the right moment), whether one tab's view may follow (`decideViewSync` -- a dirty editor HOLDS rather than being rebuilt, since rebuilding is how the three views re-root and it destroys the buffer), the two widths a toggle sets and the floor under them (`dockBoxes` -- the column is 0 when closed AND when STARVED: the panel inside is laid out at a fixed width, so a row too narrow to seat a readable dock would show a cropped sliver of it, and readable-or-absent is the honest end of that trade; every bound here is against the room the dock and the grid SHARE, never the workspace, which is larger by whatever #sessions is taking), and the persisted prefs. See doc/design/side-dock.md
  sessionstore.ts   the app's ONE session list: single-flight scan sharing so no two consumers can scan the disk at once (#493) (DOM-free, unit-tested). See doc/design/session-index.md
  auditstore.ts     the PANE's ONE `orch_audit` read (#1317): the audit viewer and the progress timeline are two views on one file that each fetched it, parsed it and held their own 5000-row copy of it at the same 1.5s follow cadence. sessionstore.ts's single-flight pattern plus a FRESHNESS WINDOW, which is the part single-flight alone cannot do -- two views following one log at one cadence rarely have their ticks land inside each other, so `AUDIT_READ_MAX_AGE_MS` (under the follow cadence, or a view would be served its own previous read) is what makes the second view's tick free. An explicit gesture passes 0 and is never served a cached answer; a failed read keeps the last good rows, does not throw into either view, and does not latch. Both views hold a REFERENCE to its array, never a copy. DOM-free, unit-tested. See doc/design/polled-payload-shapes.md
  sessionmeta.ts    pure session-browser task/repo-branch/PR formatting + truncation (#1) (DOM-free, unit-tested)
  resumeerror.ts    pure classification of a resume failure's structured backend tag into a UI affordance -- start-fresh vs a plain error (#412) (DOM-free, unit-tested)
  restorecard.ts    pure dormant-card lifecycle state machine -- idle/pending/error, click acknowledged immediately, failure always lands on a persistent error state (#479) (DOM-free, unit-tested)
  launcher.ts       in-pane welcome / pane-setup form (Agent / Orchestrator / Terminal / File-explorer / File-editor / Git / Workflow / SSH kind picker; the SSH section is also the inline profile editor -- what it launches is what it saves)
  modelcatalog.ts   pure "which models may this CLI be pinned to?" (#935): the curated-suggestions/backend-probe merge (probe leads, curated backs it, inherit row pinned first), the picker's selection rule, and the injected-probe memo both surfaces share. States no vendor fact of its own. DOM-free, unit-tested. See doc/design/model-catalog.md
  modelpicker.ts    the model dropdown + `custom…` escape, shared by the launcher's per-role selector and the workflow pane's block editor (#935) -- DOM wiring only, every decision it makes is modelcatalog.ts's
  modelprobe.ts     the app's ONE ModelCatalog instance (#935), wired to pty.ts's probeAgentCli -- separate from modelcatalog.ts, which must stay reachable from `node --test`. A launcher pane BECOMES a workflow pane, and a per-form catalog would re-probe every CLI across that handover. App scope has no natural expiry, so the memo keeps only what cliprobe.rs would have cached (`worthKeeping`): a front memo holding a failure the backend refuses to hold makes the backend's own "a CLI installed mid-session must become launchable" unreachable
  modelcontext.ts   the family->context-window table (#993) -- the one model fact loomux states on its own authority, because no CLI emits the number (they say it in banner prose) and the machine-readable source needs a vendor API key. Keyed by CLI then id, family aliases first, and SILENT where no page says otherwise. DOM-free, unit-tested. See doc/design/model-catalog.md
  modelwire.ts      reads a CLI's own list-models control reply off stdout (#993) into ModelDetail rows (real ids, displayName, resolvedModel, per-model effort levels). May under-recognise, never manufactures: every id is a verbatim `value` from the CLI's JSON, and no number is scraped out of the description prose. The backend half (src-tauri/src/modelwire.rs) transports bytes and parses nothing. DOM-free, unit-tested
  workflowknobs.ts  the workflow block editor's effort/context rows as a live function of the model the picker holds NOW (#935), so a typed model id re-derives them without re-rendering the form over the human's caret. Injected KnobLookup; DOM-free, unit-tested. See doc/design/model-catalog.md
  panesetup.ts      pure kind-selection + validation core for the welcome screen (DOM-free, unit-tested). Also the SSH launch seam (#887): the S1->S2 compose, the refusals for values the profile store would silently discard, and `sshOrchestrationRefusal` -- the #887/#888 boundary, which reads the spawn options and the pane's own state by ONE union rule. See doc/design/ssh-panes.md
  orchestration.ts  frontend half of agent groups (panes, badges, focus); also the human-only cross-workspace channel commands (connect/disconnect/set-sender, standalone-pane prepare/bind/adopt) + `orch-channel` event routing (#271)
  shortcuts.ts      app-level keybindings (single source of truth)
  nativedialog.ts   the app's ONE gate on native (OS-owned) dialogs (#1564): at most one folder picker outstanding, and no window-focus reclaim while one is. The picker is shown on a thread the dialog plugin spawns while our main window OWNS it, so its SetFocus re-enters our window procedure and WebView2's focus machinery synchronously -- the app's two contributions to that path (a second dialog, and pushing focus back into the terminal from inside a focus event) are what this refuses. Reuses SubmitLatch; DOM-free, unit-tested. See doc/design/native-dialog-focus.md
  fileapi.ts        typed bridge to fileedit.rs (per-feature wrapper, like git.ts) + `admitRoot` (#1042 slice B), the one wrapper over `admit_root` every admit site calls — a picker result, a launcher submit, a restored root, a human view-open gesture on a pane's cwd. Never the OSC-7 report itself, which is agent-controllable and resolve-only
  fileedit.ts       the file editor (#174): tree + code editor + "Go to file" name search + content search/replace. Two hosts: the Alt+F overlay, and an editor PANE (#217, `embedded`) (DOM wiring)
  fileexplorer.ts   the file MANAGER a files pane hosts (#214): browse, open-with-default-app, new file/folder, rename, delete, context menu, SHA-256 column, Go to file (DOM wiring)
  fileexplorermodel.ts pure file-manager core: listing order, rooted navigation, breadcrumb, formatting, inline-edit validation, op-target binding (DOM-free, unit-tested)
  filemenu.ts       pure context-menu model: what appears, what it acts on (target bound at menu-open) (DOM-free, unit-tested)
  contextmenu.ts    generic context-menu renderer, `MenuItem<A>`: placement, submenus, Esc/click-away (DOM wiring)
  panemenu.ts       pure pane-header connect-menu model (#271): Connect/directional-completion/Join-as-receiver/Cancel/Disconnect/Make-sender per pane + pending-arm state, standalone panes included (DOM-free, unit-tested)
  pasteflow.ts      pure terminal copy/paste keydown gesture decisions (#370): Ctrl+V/Ctrl+Shift+V + Ctrl+C (selection-gated)/Ctrl+Shift+C key matching, the copy/paste/pass disposition enum that drives pane.ts's preventDefault() calls -- same for every pane kind, no pane-kind branch (DOM-free, unit-tested)
  channel.ts        pure connect-gesture reducer (arm/complete/cancel/set-sender) + per-channel color/number/direction chip derivation (#271) (DOM-free, unit-tested)
  filehashmodel.ts  pure hashing policy: auto-hash threshold, digest cache keying (path+size+mtime), formatting (DOM-free, unit-tested)
  filemgr.ts        typed bridge to filemgr.rs + filehash.rs (per-feature wrapper, like fileapi.ts)
  filematch.ts      pure file-NAME matching + ranking for "Go to file" (#214, DOM-free, unit-tested)
  modal.ts          the shared confirm/choice dialog (used by the editor and the file manager)
  filetreemodel.ts  pure lazy-tree model: sort/merge/flatten (DOM-free, unit-tested)
  fileicons.ts      pure filename -> inline-SVG icon mapping (DOM-free, unit-tested)
  searchresults.ts  pure search grouping + tree-hit + replace-selection model (DOM-free, unit-tested)
  searchsession.ts  pure streaming-search state machine: batch/cancel + result cap + enumeration-source pick (#207, DOM-free, unit-tested)
  dirtystate.ts     pure conflict/close-guard decisions -- shared by the editor's Esc/close and the editor PANE's close (#217) (DOM-free, unit-tested)
  eol.ts            pure line-ending detect/normalize/re-apply for EOL-safe dirty tracking (unit-tested)
  findwidget.ts     pure in-file-find logic: regex build + "n of m" match count (DOM-free, unit-tested)
  editorwidget.ts   swappable editor widget: lazy CodeMirror 6 (One Dark) + custom find panel + textarea fallback
  voice.ts          pure voice logic: target decision + push-to-talk state machine
  voicecontrol.ts   global single-capture controller; routes transcripts to focus
  main.ts           composition root (owns the TabManager + OrchWiring router)
e2e/                Playwright E2E PoC (experimental, `e2e-windows` CI job) — see doc/design/e2e-testing.md
npm/bin/orrerix.js  the whole `orrerix` npm package: a dependency-free Node launcher that fetches/installs/launches the desktop app. Command-based (#845) — plain `orrerix` never installs over an existing install, only `orrerix update` does; `update` is channel-aware, orders releases by semver itself (never GitHub's mutable `latest` pointer) and refuses a downgrade outright (#815/#816/#846). Renamed from `loomux-desktop`/`loomux.js` in #1153 phase 5, which is also why it reads TWO name axes: the product (`Orrerix.app`, the install dir, the NSIS uninstall key, the asset prefix) and the main binary (`loomux`, cargo's output, unrenamed) — every reading surface accepts both product spellings, doc/design/rebrand-external.md. Pure logic unit-tested in test/launcher.test.ts. See doc/design/npm-launcher.md
```

## Extension seams

New agent sources add a `scan_*` in `sessions.rs`; new backend
capabilities add a `#[tauri::command]` plus a typed wrapper in `pty.ts` — or, for
a self-contained feature, a dedicated wrapper module (`git.ts`, `gh.ts`,
`fileapi.ts`). Either way the wrapper reaches the backend through `transport.ts`,
which is the only module that imports `@tauri-apps` (constraint 5, enforced by
`test/transport.test.ts`); a NEW Tauri API — not a new command, a new *API* —
is added to the `EngineTransport` interface there and nowhere else. **A new
command also needs an ACL grant** (#363): add its bare name to
`command_manifest::APP_COMMANDS`, then grant it to `main` — directly in
`capabilities/default.json` or via one of the sets under `permissions/sets/`
aggregated into `main-ui`. `tests/acl_manifest.rs` fails loudly if either step
is missed; see doc/design/acl-manifest.md. A new command, event listener or
timer must also satisfy the webview-thread responsiveness invariants, which
`tests/perf_dispatch.rs` and `test/perfpolicy.test.ts` will enforce against a
declared manifest (#743 S2/S3) — see doc/design/performance.md.
